# html-to-markdown

HTML to Markdown converter — zero dependencies.

## For AI agents
- scripts/convert.py is the main entrypoint
- Supports URL, file, and stdin input
- JSON mode with --json for machine parsing
- Strips scripts, styles, nav, header, footer automatically
